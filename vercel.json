{
  "buildCommand": "cd frontend/user-app && npm install && npm run build",
  "outputDirectory": "frontend/user-app/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}